# Stimulus Mapping

## Mapping Table

| Condition | Stage/Phase | Stimulus IDs | Participant-Facing Content | Source Paper ID | Evidence (quote/figure/table) | Implementation Mode | Asset References | Notes |
|---|---|---|---|---|---|---|---|---|
| `inclusion` | avatar_turn | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line`, `avatar_wait_prompt` | Three-player scene where an avatar holds the ball and decides whom to toss to; participant is repeatedly included over time. | W2048818633 | Inclusion condition maintains participant involvement in ball circulation. | psychopy_builtin | `config/*.yaml -> stimuli.participant_node/left_node/right_node/*_label/ball/status_line/avatar_wait_prompt` | Holder node is highlighted at runtime. |
| `inclusion` | participant_turn | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line`, `participant_prompt` | Participant receives ball and chooses toss target (`left`/`right`) via configured keys. | W2048818633 | Participant actively chooses recipient when in possession of ball. | psychopy_builtin | `config/*.yaml -> stimuli.participant_prompt` plus scene stimuli | Prompt wording is config-driven for localization. |
| `inclusion` | toss_animation | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line` | Ball appears at destination node and toss transition is event-coded. | W2107794155 | Toss-transfer epochs are represented as distinct events in neuro/behavioral workflows. | psychopy_builtin | `config/*.yaml -> stimuli.ball/status_line` | Toss start trigger is destination-specific (`to_participant/left/right`). |
| `exclusion` | avatar_turn | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line`, `avatar_wait_prompt` | Avatar-only passing after initial inclusion threshold; participant observes exclusion phase. | W2463168384 | Exclusion manipulation involves reduced participant receipt after initial throws. | psychopy_builtin | `config/*.yaml -> scene stimuli + avatar_wait_prompt` | Controller enforces exclusion schedule via `exclusion_initial_receives`. |
| `exclusion` | participant_turn | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line`, `participant_prompt` | If participant receives ball early in exclusion block, participant still makes left/right toss choice. | W2463168384 | Early participant involvement precedes full exclusion in many variants. | psychopy_builtin | `config/*.yaml -> participant_prompt + scene stimuli` | Participant turns may disappear later in exclusion block. |
| `exclusion` | toss_animation | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line` | Toss transitions continue while participant becomes less frequently targeted. | W2048818633 | Ostracism manipulation is operationalized through toss allocation patterns. | psychopy_builtin | `config/*.yaml -> ball/status_line + scene stimuli` | Same visual layout as inclusion to isolate social manipulation. |
| `all_conditions` | inter_toss | `participant_node`, `left_node`, `right_node`, `participant_label`, `left_label`, `right_label`, `ball`, `status_line` | Brief pause with updated holder before next toss event. | W2048818633 | Continuous game pacing includes short intervals between tosses. | psychopy_builtin | `config/*.yaml -> scene stimuli + status_line` | Implemented as explicit `inter_toss` phase for timing control. |
| `all_conditions` | envelope | `instruction_text`, `block_break`, `good_bye` | Instructions and session summaries reporting toss counts and participant involvement metrics. | W2048818633 | Standard Cyberball implementations include briefing and debriefing envelopes. | psychopy_builtin | `config/*.yaml -> stimuli.instruction_text/block_break/good_bye` | No participant-facing trial text is hardcoded in `run_trial.py`. |
